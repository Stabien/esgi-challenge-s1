<form method="post" action="{{ path('app_admin_team_player_delete', {'id': team_player.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team_player.id) }}">
    <button class="btn">Delete</button>
</form>
