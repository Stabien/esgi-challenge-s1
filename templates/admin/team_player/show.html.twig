{% extends 'base.html.twig' %}

{% block title %}TeamPlayer{% endblock %}

{% block body %}
    <h1>TeamPlayer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team_player.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ team_player.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ team_player.lastname }}</td>
            </tr>
            <tr>
                <th>Nickname</th>
                <td>{{ team_player.nickname }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ team_player.role }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ team_player.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_team_player_index') }}">admin to list</a>

    <a href="{{ path('app_admin_team_player_edit', {'id': team_player.id}) }}">edit</a>

    {{ include('admin/team_player/_delete_form.html.twig') }}
{% endblock %}
