{% extends 'base.html.twig' %}

{% block title %}Team{% endblock %}

{% block body %}
    <h1>Team</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ team.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ team.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_team_index') }}">admin to list</a>

    <a href="{{ path('app_admin_team_edit', {'id': team.id}) }}">edit</a>

    {{ include('admin/team/_delete_form.html.twig') }}
{% endblock %}
