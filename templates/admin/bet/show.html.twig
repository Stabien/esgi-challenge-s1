{% extends 'base.html.twig' %}

{% block title %}Bet
{% endblock %}

{% block body %}
<div class="flex items-center gap-8 pl-8">
	<a class="  title text-blue-150 inline-block w-fit"  href="{{ path('app_admin_bet_index') }}">admin to list</a>
	{# <h1>Bet</h1> #}
</div>

	<table class="table mx-auto mb-8">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ bet.id }}</td>
			</tr>
			<tr>
				<th>Amount</th>
				<td>{{ bet.amount }}</td>
			</tr>
			<tr>
				<th>Status</th>
				<td>{{ bet.status }}</td>
			</tr>
			<tr>
				<th>Earnings</th>
				<td>{{ bet.earnings }}</td>
			</tr>
		</tbody>
	</table>

  <div class="flex items-center justify-center mt-8 gap-8">
	<a class=" btn gold" href="{{ path('app_admin_bet_edit', {'id': bet.id}) }}">edit</a>

	{{ include('admin/bet/_delete_form.html.twig') }}
    </div>

{% endblock %}
