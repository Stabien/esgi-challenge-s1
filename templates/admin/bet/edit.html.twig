{% extends 'base.html.twig' %}

{% block title %}Edit Bet{% endblock %}

{% block body %}
    <h1 class="title small">Edit Bet</h1>

    {{ include('admin/bet/_form.html.twig', {'button_label': 'Update'}) }}


<div class="flex items-center justify-center mt-8 gap-8">
    <a class=" btn gold" href="{{ path('app_admin_bet_index') }}">admin to list</a>
    {{ include('admin/bet/_delete_form.html.twig') }}
    </div>

{% endblock %}
