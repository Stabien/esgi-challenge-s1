<form method="post" action="{{ path('app_bet_delete', {'id': bet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bet.id) }}">
    <button class="btn">Delete</button>
</form>
